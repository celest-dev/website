---
title: Celest | The Flutter cloud platform
description: Celest is a Flutter cloud platform that allows you to build, deploy, and manage backends for your Flutter app with ease.
---

import { Tabs, Steps } from 'nextra/components'
import { Bleed } from 'nextra-theme-docs'
import Link from 'next/link'
import Image from 'next/image'

import Hero from '@components/landing/Hero'
import Newsletter from '@components/landing/Newsletter'
import Content from '@components/landing/Content'
import { Download } from '@components/Download'
import { Feature, FeatureSet } from '@components/landing/Feature'

<Hero />

<Bleed>
<FeatureSet>
  <Feature 
    title="All your backend logic in Dart"
    description={<>Annotate any Dart function with <code>@cloud</code> to turn it into a serverless function.</>}
  >
```dart
@cloud
Future<String> sayHello(String name) async {
  return 'Hello, $name!';
}
```
  </Feature>
  <Feature 
    title={<>Out-of-the-box database</>}
    description="Celest takes your Drift schema and automatically creates a database for you."
  >
  <div className="flex flex-row justify-center items-stretch flex-wrap">

<div className="h-full m-2">
```dart filename="database.dart"
import 'package:drift/drift.dart';

class Tasks extends Table {
  late id = integer().autoIncrement()();
  late title = text()();
  late completed = boolean()();
}
```
</div>
<div className="h-full m-2">
```dart filename="project.dart"
import 'package:celest/celest.dart';

const tasks = Database(
  schema: Schema.drift(TaskDatabase),
);
```
</div>
</div>
  </Feature>
  <Feature 
    title="Deploy in a single command"
    description="When you're ready to productionize, run `celest deploy`. That's it!"
  >
```ansi
$ celest deploy
[0m [0;32m✓ [0mInitializing Celest (5ms)
[0m [0;32m✓ [0mProject successfully migrated (1.7s)
[0m [0;32m✓ [0m🔥 Warming up the engines (12.3s)
[0m [0;32m✓ [0m✨ Contacting the Celestials (10.0s)
[0m [0;32m✓ [0m🚀 Deploying to Celest Cloud (38.7s)
💙 [0m [0;32mYour Celest project has been deployed![0m
https://my-project-a18bbb.us-south1.celest.cloud
```
  </Feature>
</FeatureSet>

</Bleed>

<hr className="my-12" />
<Content />
